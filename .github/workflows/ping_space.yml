name: Ping Space
on:
  schedule:
    - cron: "0 12 * * *"  # Runs every day at 12:00 UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: curl -sL "https://blinkit-discovery-engine-zxvmfgkrzhmodja7ubg3bm.streamlit.app" > /dev/null
